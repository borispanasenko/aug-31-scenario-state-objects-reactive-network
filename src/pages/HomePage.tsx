import CounterAppContainer from '../components/CounterAppContainer';

const HomePage = () => {
    return (
        <div className="min-h-screen bg-surface text-foreground">
            <CounterAppContainer />
        </div>
    );
};

export default HomePage;